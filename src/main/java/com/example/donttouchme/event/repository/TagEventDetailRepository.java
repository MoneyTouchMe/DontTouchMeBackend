package com.example.donttouchme.event.repository;

import com.example.donttouchme.event.domain.TagEventDetail;
import org.springframework.data.jpa.repository.JpaRepository;

public interface TagEventDetailRepository extends JpaRepository<TagEventDetail, Long> {
}
